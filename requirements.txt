# Core
numpy>=1.26
pandas>=2.2
scipy>=1.12
matplotlib>=3.8

# Copulas + friends (pure-Python)
copulas>=0.11.0

# IO / CLI / extras
openpyxl>=3.1
tqdm>=4.66

# Notebook + widgets (only if you need the interactive viz)
jupyterlab>=4.0
ipywidgets>=8.1
